info:
  name: create magic link
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/magic-link
  body:
    type: json
    data: |-
      {
        "email": "admin@processmonitor.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
